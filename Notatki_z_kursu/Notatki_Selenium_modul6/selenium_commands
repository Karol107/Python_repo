driver.close() -zamyka tylko jedno oknto obecnie w tescie
driver.quit() -zamyka wszystko jak leci

driver.get('http..costam') przechodzimyu pod adres

<input type = 'text' name= 'password' id='some_id' />

element = driver.find_element_by_name('password') selektor po name

element = driver.find_element_by_id('some_id') selektor po id

element = driver.find_element_by_xpath('//input{@id+some_id"]') selektor po xpath

